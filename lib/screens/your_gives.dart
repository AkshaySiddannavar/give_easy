import 'package:flutter/material.dart';

class YourGivesScreen extends StatelessWidget {
  static const String id = 'your_gives_screen';
  const YourGivesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
